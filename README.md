# Cthulhu Web Client

## Development mode

The Frontend Client side code can run using its Makefile.

```bash
# Navigate to service root
make dev
```

Or you can run it using npm directly

```bash
# Navigate to service root
npm run dev
```
